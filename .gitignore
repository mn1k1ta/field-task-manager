# ---- .NET ----
bin/
obj/
*.user
.vs/

# ---- EF Core / SQLite runtime database ----
*.db
*.db-shm
*.db-wal

# ---- Node / Angular ----
node_modules/
client/.angular/
dist/

# ---- Angular SPA build output (regenerated by `dotnet run`) ----
server/wwwroot/

# ---- BMAD tooling (framework + IDE skills; regenerable via `npx bmad-method install`) ----
# NOTE: _bmad-output/ (planning artifacts: brief, PRD, architecture, ux-spec, epics) IS kept.
_bmad/
.claude/

# ---- OS / editor ----
Thumbs.db
.DS_Store
*.swp
